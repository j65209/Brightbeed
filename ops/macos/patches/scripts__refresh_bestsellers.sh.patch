--- a/scripts/refresh_bestsellers.sh
+++ b/scripts/refresh_bestsellers.sh
@@ -1,10 +1,4 @@
 #!/bin/bash
-# 30분마다 launchd 가 호출 → sixshop_bestsellers.py all 실행 → out/bestsellers_all.json 갱신.
-# proxy 의 GET /sx/bestsellers?brand=all 이 이 캐시 파일을 즉시 서빙 (100ms 이내).
-#
-# 주의: 이 스크립트는 admin 4브랜드(6a/yar/kop/apt) Playwright 로그인이 병렬로 돌아
-# 실제 실행 시 4-8초 걸린다. Pro 브랜드는 pro_*_sales.json 캐시 리셰이프라 즉시.
-# refresh_pro_sales.sh 가 먼저 돌면 Pro 캐시가 최신 → 이 스크립트가 잡히는 데이터도 최신.
-set +e
-cd ~/server/brightbeed-proxy
-/usr/bin/python3 scripts/sixshop_bestsellers.py all
+set -euo pipefail
+SCRIPT_DIR="$(cd -- "$(dirname -- "$0")" && pwd)"
+exec /usr/bin/python3 "$SCRIPT_DIR/job_runtime.py" bestsellers
