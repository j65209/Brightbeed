--- a/scripts/refresh_pro_sales.sh
+++ b/scripts/refresh_pro_sales.sh
@@ -1,16 +1,4 @@
 #!/bin/bash
-# 30분마다 launchd가 호출 → 식스샵프로 브랜드 모두 순회 fetch.
-# sales + stock 함께 갱신 (stock 은 판매순위 뷰의 썸네일/URL 매칭용).
-# 브랜드 리포트는 refresh_brand_report.sh(매일 07:00) 로 분리 — 여기서는 실행하지 않는다.
-#
-# 로그인 페이싱: 한 사이클에 4 브랜드 × 2회(sales+stock) = 8회 sign-in 이 몰리면
-# sixshop 이 SM-LOGIN-009 (rate limit) 로 500 을 리턴한다. 각 로그인 사이 8초 대기.
-set +e
-cd ~/server/brightbeed-proxy
-for brand in akoi pp ct koz; do
-    /usr/bin/python3 scripts/sixshop_pro_sales.py "$brand"
-    sleep 8
-    /usr/bin/python3 scripts/sixshop_pro_stock.py "$brand"
-    sleep 8
-    echo "---"
-done
+set -euo pipefail
+SCRIPT_DIR="$(cd -- "$(dirname -- "$0")" && pwd)"
+exec /usr/bin/python3 "$SCRIPT_DIR/job_runtime.py" pro-sales
