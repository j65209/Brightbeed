--- a/scripts/refresh_admin_sales.sh
+++ b/scripts/refresh_admin_sales.sh
@@ -1,9 +1,4 @@
 #!/bin/bash
-# 30분마다 launchd가 호출 → 식스샵 일반 admin 브랜드 모두 순회.
-# 브랜드 리포트(admin_report.py)는 refresh_brand_report.sh(매일 07:00)로 분리 — 여기서 실행하지 않는다.
-set +e
-cd ~/server/brightbeed-proxy
-for brand in 6a yar kop apt; do
-    /usr/bin/python3 scripts/sixshop_admin_sales.py "$brand"
-    echo "---"
-done
+set -euo pipefail
+SCRIPT_DIR="$(cd -- "$(dirname -- "$0")" && pwd)"
+exec /usr/bin/python3 "$SCRIPT_DIR/job_runtime.py" admin-sales
